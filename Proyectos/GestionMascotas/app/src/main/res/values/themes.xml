<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base para el modo claro -->
    <style name="Base.Theme.GestionMascotas" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#bd793f</item>  <!-- Color principal claro -->
        <item name="colorPrimaryVariant">#C88C5C</item>  <!-- Variante del color principal -->
        <item name="colorOnPrimary">#FFFFFF</item>  <!-- Color del texto sobre el color primario -->
        <item name="android:statusBarColor">#bd793f</item>  <!-- Color de la barra de estado -->
        <item name="android:windowBackground">#faebda</item>  <!-- Fondo de la ventana -->
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#757575</item>
        <item name="android:buttonStyle">?android:attr/buttonStyle</item>
    </style>

    <!-- Tema base para el modo oscuro -->
    <style name="Base.Theme.GestionMascotas.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#4B2C20</item>
        <item name="colorPrimaryVariant">#6C3F28</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="android:statusBarColor">#4B2C20</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textColorSecondary">#B0B0B0</item>
        <item name="android:buttonStyle">?android:attr/buttonStyle</item>

    </style>

    <!-- Tema final para la aplicación -->
    <style name="Theme.GestionMascotas" parent="Base.Theme.GestionMascotas" />

    <style name="Theme.GestionMascotas.DarkMode" parent="Base.Theme.GestionMascotas.Dark" />
</resources>
