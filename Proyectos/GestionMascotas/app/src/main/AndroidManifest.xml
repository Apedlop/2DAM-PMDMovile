<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionMascotas">
        <activity
            android:name=".EditarMascota"
            android:exported="false" />
        <!-- Actividad de Listado de Mascotas para Usuario -->
        <activity
            android:name=".ListadoMascotasUser"
            android:exported="false" /> <!-- Actividad de Listado de Mascotas para Administrador -->
        <activity
            android:name=".ListadoMascotasAdmin"
            android:exported="false" /> <!-- Actividad de Información Detallada -->
        <activity
            android:name=".InfoDetallada"
            android:exported="false" /> <!-- Actividad Principal (Inicio de sesión) -->
        <activity
            android:name=".InicioSesion"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>